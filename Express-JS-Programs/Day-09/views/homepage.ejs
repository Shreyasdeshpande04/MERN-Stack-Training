<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student Report</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: white;
      font-family: Arial, sans-serif;
    }

    h2 {
      font-weight: bold;
      color: #333;
    }

    .card {
      border-radius: 12px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border: none;
    }

    .card:hover {
      transform: scale(1.03);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }

    .card-body {
      padding: 20px;
    }

    .card-title {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .card-text {
      font-size: 15px;
      margin: 4px 0;
    }


    .card-color-1 { background-color: #76b7e6; }  
    .card-color-2 { background-color: #ec729b; }  
    .card-color-3 { background-color: #9de2a2; } 
    .card-color-4 { background-color: #e8cf7e; }  
    .card-color-5 { background-color: #ae84ed; }  
    .card-color-6 { background-color: #ec8c81; }  
  </style>
</head>
<body>

  <%- include('header') %><br><br>
<center><h1>WELCOME TO HOME PAGE</h1></center> <div class="container mt-5 mb-5">
    <h2 class="text-center mb-4">Placed Students Report</h2><div class="row">
      <% users.forEach((user, index) => {const colorClass = `card-color-${(index % 6) + 1}`;%>
      <div class="col-md-4 mb-4"><div class="card <%= colorClass %> shadow-sm"><div class="card-body">
              <h5 class="card-title text-dark"><%= user.name %></h5>
              <p class="card-text"><strong>Email:</strong> <%= user.email %></p>
              <p class="card-text"><strong>Degree:</strong> <%= user.degree %></p>
              <p class="card-text"><strong>Domain:</strong> <%= user.domain %></p>
              <p class="card-text"><strong>Company:</strong> <%= user.company %></p>
              <p class="card-text"><strong>Role:</strong> <%= user.role %></p>
              <p class="card-text"><strong>Package:</strong> ₹<%= user.packagee %> LPA</p>
            </div></div></div>
            <% }) %>
          </div></div>
  <%- include('footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
